#include<stdio.h>
#include<string.h>

#define MAX 1000
#define KRED  "\x1B[31m"

int main(int argc, char *argv[])
{
	FILE *fp;
	fp = fopen(argv[2], "r");

	char string[MAX];

	while(fgets(string, MAX, fp)!=EOF)
	if(strstr(string, argv[1])!=NULL)
		fputs(string, stdout);
}
